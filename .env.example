# Leave both AtlasPay variables unset to run the deterministic fixture demo.
# Configure both together to enable live fail-closed AtlasPay mode.
ATLASPAY_API_BASE_URL=
ATLASPAY_API_TOKEN=
ATLASPAY_API_TIMEOUT_MS=3000
